{
  "name": "frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm i --include=dev && dfx generate backend",
    "build": "vite build",
    "dev": "vite"
  },
  "dependencies": {
    "@dfinity/agent": "^2.1.3"
  },
  "devDependencies": {
    "vite": "5.4.11",
    "vite-plugin-environment": "1.1.3"
  }
}
